apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: ybb-platform
spec:
  selector:
    app: file-service
  ports:
  - protocol: TCP
    port: 8001
    targetPort: 8001
  type: ClusterIP
